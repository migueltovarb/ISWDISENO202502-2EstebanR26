# Server Configuration
server.port=8080
server.servlet.context-path=/api

# MongoDB Configuration - MongoDB Atlas
spring.data.mongodb.uri=mongodb+srv://esteban_26:XBqIW5MvfdgetGja@cluster0.tffl1uq.mongodb.net/Aacademix?retryWrites=true&w=majority
spring.data.mongodb.database=Aacademix

# JWT Configuration
jwt.secret=AcademiXSecretKeyForJWTTokenGenerationAndValidation2024
jwt.expiration=86400000

# Email Configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=your-email@gmail.com
spring.mail.password=your-app-password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Stripe Configuration
stripe.api.key=sk_test_your_stripe_secret_key

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging
logging.level.com.academix=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG

# CORS Configuration
cors.allowed-origins=http://localhost:3000,http://localhost:5500

# CORS Configuration
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.max-age=3600